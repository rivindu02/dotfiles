# Install to /etc/systemd/system/user@.service.d/
# Kill the worst offender in the desktop session under sustained memory pressure
[Service]
ManagedOOMMemoryPressure=kill
ManagedOOMMemoryPressureLimit=50%
